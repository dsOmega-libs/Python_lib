watch_file ../../pixi.lock

PIXI_ENV="${PIXI_ENV:-common}"

source_env_if_exists .envrc.local

echo "Activating package pixi environment: $PIXI_ENV"
eval "$(pixi shell-hook --environment "$PIXI_ENV" --manifest-path ../.. --frozen)"
